# Output

This directory is intentionally free of submission artifacts in Phase 0.
`matching_results.tsv` and `candidate_pairs.tsv` must not be created until the
official dataset, output schema, and validator have been inspected.
