# Configuration Variants

`config.yaml` is the active Phase 0 configuration. Add reviewed, named
configuration variants here only after the dataset audit establishes the
allowed schema-dependent settings. Do not encode filenames, fields, country
lists, thresholds, or model parameters from assumptions.
