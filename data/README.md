# Data Directory

Challenge data is intentionally excluded from version control. The directory
layout is prepared for supplied raw, processed, train, validation, and test
artifacts, but no filename or schema is assumed. Preserve the raw supplied
files unchanged once they arrive.
